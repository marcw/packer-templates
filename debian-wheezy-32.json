{
    "builders": [
        {
            "type": "vmware",
            "iso_url": "/Users/ash/ISO/debian-7.1.0-i386-CD-1.iso",
            "iso_md5": "c29ec2db6754dadd341b9763164aca9a",
            "disk_size": 5000,
            "ssh_username": "packer",
            "ssh_password": "packer",
            "guest_os_type": "debian6",
            "headless": false,
            "http_directory": ".",
            "boot_wait": "5s",
            "boot_command": ["<esc><wait>auto url=http://{{ .HTTPIP }}:{{ .HTTPPort }}/preseed.cfg<enter>"]
        },
        {
            "type": "virtualbox",
            "iso_url": "/Users/ash/ISO/debian-7.1.0-i386-CD-1.iso",
            "iso_md5": "c29ec2db6754dadd341b9763164aca9a",
            "ssh_username": "packer",
            "ssh_password": "packer",
            "guest_os_type": "Debian",
            "headless": false,
            "http_directory": ".",
            "boot_wait": "5s",
            "boot_command": ["<esc><wait>auto url=http://{{ .HTTPIP }}:{{ .HTTPPort }}/preseed.cfg<enter>"]
        }
    ],

    "provisioners": [
        {
            "type": "shell",
            "script": "provision.bash",
            "execute_command": "{{ .Vars }} sudo bash {{ .Path }}"
        }
    ],

    "post-processors": [
        "vagrant"
    ]
}
